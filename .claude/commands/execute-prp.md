# Execute PRP

Execute the PRP implementation workflow.

## PRP File: $ARGUMENTS

Run the following command to begin the automated execution process:

```
@.claude/scripts/execute/initiate-execution.sh "$ARGUMENTS"
```

This will initiate the sequential sub-agent implementation system.
